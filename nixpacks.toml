# nixpacks.toml — repo root
# Nix's Python is externally managed, so we must create a venv first.

[phases.setup]
nixPkgs = ["python311"]
aptPkgs = ["libgomp1", "libstdc++6"]

[phases.install]
cmds = [
  "python3 -m venv /app/.venv",
  "/app/.venv/bin/pip install --upgrade pip",
  "/app/.venv/bin/pip install -r backend/requirements.txt",
  "/app/.venv/bin/python -c 'from chromadb.utils import embedding_functions; embedding_functions.ONNXMiniLM_L6_V2()'"
]

[start]
cmd = "cd backend && /app/.venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
