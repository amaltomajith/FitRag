# railway.toml — Railway reads this for deploy settings.
# Build is handled by the Dockerfile in the repo root.
[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
